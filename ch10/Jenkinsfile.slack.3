@Library('my-global-shared-lib') _

pipeline {
    agent any

    stages {
        stage('Hello Slack!') {
            steps {
                script {
                    slack.send(
                        "#general",
                        "Hello slack.send(String, String)!")
                }
            }
        }
    }
}
